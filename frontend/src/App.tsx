/**
 * Phase 3B.2 foundation only -- proves the React/TypeScript/Vite shell
 * renders. Deliberately does NOT yet render a ClassSection selector, the
 * timetable grid, or any live API data -- that is Phase 3B.3's job
 * (`docs/DECISIONS.md` #32).
 */
function App() {
  return (
    <main className="app-shell">
      <h1>School Timetable</h1>
      <p className="app-subtitle">The timetable view is being prepared.</p>
    </main>
  );
}

export default App;
